# 第15章 命令行参数参考

> 文档版本：V1.0.0

本章按登记基线的主命令帮助整理参数。扩展可以注册附加参数，因此实际输出可能更多。

参数值、互斥关系和默认行为应以目标安装环境执行 loushang --help 的结果为准。

## 15.1 `--help, -h`

- 用途：显示主命令帮助并退出。
- 适用场景：安装验证和参数发现。
- 查询方式：运行 `loushang --help` 核对 `--help, -h` 的登记基线定义。
- 自动化要求：固定结果格式，并同时检查退出状态和标准错误。
- 安全要求：涉及写入、外部访问或删除时应先确认范围。
- 兼容性：扩展参数和提供方参数可能随已加载资源变化。

## 15.2 `--version, -v`

- 用途：显示程序版本并退出。
- 适用场景：核对部署与登记基线。
- 查询方式：运行 `loushang --help` 核对 `--version, -v` 的登记基线定义。
- 自动化要求：固定结果格式，并同时检查退出状态和标准错误。
- 安全要求：涉及写入、外部访问或删除时应先确认范围。
- 兼容性：扩展参数和提供方参数可能随已加载资源变化。

## 15.3 `--mode`

- 用途：选择 text、print、json、rpc 或 channel 模式。
- 适用场景：按终端或集成调用方选择输出协议。
- 查询方式：运行 `loushang --help` 核对 `--mode` 的登记基线定义。
- 自动化要求：固定结果格式，并同时检查退出状态和标准错误。
- 安全要求：涉及写入、外部访问或删除时应先确认范围。
- 兼容性：扩展参数和提供方参数可能随已加载资源变化。

## 15.4 `--tui`

- 用途：请求启动终端交互产品面。
- 适用场景：多轮编程工作。
- 查询方式：运行 `loushang --help` 核对 `--tui` 的登记基线定义。
- 自动化要求：固定结果格式，并同时检查退出状态和标准错误。
- 安全要求：涉及写入、外部访问或删除时应先确认范围。
- 兼容性：扩展参数和提供方参数可能随已加载资源变化。

## 15.5 `--no-tui`

- 用途：关闭全屏终端界面。
- 适用场景：脚本、管道或普通文本运行。
- 查询方式：运行 `loushang --help` 核对 `--no-tui` 的登记基线定义。
- 自动化要求：固定结果格式，并同时检查退出状态和标准错误。
- 安全要求：涉及写入、外部访问或删除时应先确认范围。
- 兼容性：扩展参数和提供方参数可能随已加载资源变化。

## 15.6 `--no-session`

- 用途：关闭本次会话持久化。
- 适用场景：一次性敏感或临时任务。
- 查询方式：运行 `loushang --help` 核对 `--no-session` 的登记基线定义。
- 自动化要求：固定结果格式，并同时检查退出状态和标准错误。
- 安全要求：涉及写入、外部访问或删除时应先确认范围。
- 兼容性：扩展参数和提供方参数可能随已加载资源变化。

## 15.7 `--session-name`

- 用途：设置新会话的可读名称。
- 适用场景：便于检索和恢复。
- 查询方式：运行 `loushang --help` 核对 `--session-name` 的登记基线定义。
- 自动化要求：固定结果格式，并同时检查退出状态和标准错误。
- 安全要求：涉及写入、外部访问或删除时应先确认范围。
- 兼容性：扩展参数和提供方参数可能随已加载资源变化。

## 15.8 `--session`

- 用途：指定会话标识。
- 适用场景：精确控制会话目标。
- 查询方式：运行 `loushang --help` 核对 `--session` 的登记基线定义。
- 自动化要求：固定结果格式，并同时检查退出状态和标准错误。
- 安全要求：涉及写入、外部访问或删除时应先确认范围。
- 兼容性：扩展参数和提供方参数可能随已加载资源变化。

## 15.9 `--list-sessions`

- 用途：列出可发现会话。
- 适用场景：恢复、审计和清理前检查。
- 查询方式：运行 `loushang --help` 核对 `--list-sessions` 的登记基线定义。
- 自动化要求：固定结果格式，并同时检查退出状态和标准错误。
- 安全要求：涉及写入、外部访问或删除时应先确认范围。
- 兼容性：扩展参数和提供方参数可能随已加载资源变化。

## 15.10 `--resume, -r`

- 用途：恢复指定会话或打开交互选择器。
- 适用场景：继续历史工作。
- 查询方式：运行 `loushang --help` 核对 `--resume, -r` 的登记基线定义。
- 自动化要求：固定结果格式，并同时检查退出状态和标准错误。
- 安全要求：涉及写入、外部访问或删除时应先确认范围。
- 兼容性：扩展参数和提供方参数可能随已加载资源变化。

## 15.11 `--continue, -c`

- 用途：恢复当前项目最近会话。
- 适用场景：快速延续上次工作。
- 查询方式：运行 `loushang --help` 核对 `--continue, -c` 的登记基线定义。
- 自动化要求：固定结果格式，并同时检查退出状态和标准错误。
- 安全要求：涉及写入、外部访问或删除时应先确认范围。
- 兼容性：扩展参数和提供方参数可能随已加载资源变化。

## 15.12 `--cwd`

- 用途：设置 Agent 工作目录。
- 适用场景：从其他 shell 目录处理目标项目。
- 查询方式：运行 `loushang --help` 核对 `--cwd` 的登记基线定义。
- 自动化要求：固定结果格式，并同时检查退出状态和标准错误。
- 安全要求：涉及写入、外部访问或删除时应先确认范围。
- 兼容性：扩展参数和提供方参数可能随已加载资源变化。

## 15.13 `--provider`

- 用途：显式选择模型提供方。
- 适用场景：收窄提供方路由。
- 查询方式：运行 `loushang --help` 核对 `--provider` 的登记基线定义。
- 自动化要求：固定结果格式，并同时检查退出状态和标准错误。
- 安全要求：涉及写入、外部访问或删除时应先确认范围。
- 兼容性：扩展参数和提供方参数可能随已加载资源变化。

## 15.14 `--model`

- 用途：选择短格式或完整格式模型键。
- 适用场景：指定模型与端点。
- 查询方式：运行 `loushang --help` 核对 `--model` 的登记基线定义。
- 自动化要求：固定结果格式，并同时检查退出状态和标准错误。
- 安全要求：涉及写入、外部访问或删除时应先确认范围。
- 兼容性：扩展参数和提供方参数可能随已加载资源变化。

## 15.15 `--thinking`

- 用途：设置模型推理强度偏好。
- 适用场景：平衡复杂度、延迟与成本。
- 查询方式：运行 `loushang --help` 核对 `--thinking` 的登记基线定义。
- 自动化要求：固定结果格式，并同时检查退出状态和标准错误。
- 安全要求：涉及写入、外部访问或删除时应先确认范围。
- 兼容性：扩展参数和提供方参数可能随已加载资源变化。

## 15.16 `--tools, -t`

- 用途：设置活动工具名称集合。
- 适用场景：建立最小权限工具面。
- 查询方式：运行 `loushang --help` 核对 `--tools, -t` 的登记基线定义。
- 自动化要求：固定结果格式，并同时检查退出状态和标准错误。
- 安全要求：涉及写入、外部访问或删除时应先确认范围。
- 兼容性：扩展参数和提供方参数可能随已加载资源变化。

## 15.17 `--no-tools, -nt`

- 用途：禁用所有工具。
- 适用场景：只根据已有上下文回答。
- 查询方式：运行 `loushang --help` 核对 `--no-tools, -nt` 的登记基线定义。
- 自动化要求：固定结果格式，并同时检查退出状态和标准错误。
- 安全要求：涉及写入、外部访问或删除时应先确认范围。
- 兼容性：扩展参数和提供方参数可能随已加载资源变化。

## 15.18 `--no-builtin-tools, -nbt`

- 用途：禁用内置工具。
- 适用场景：只使用其他贡献来源。
- 查询方式：运行 `loushang --help` 核对 `--no-builtin-tools, -nbt` 的登记基线定义。
- 自动化要求：固定结果格式，并同时检查退出状态和标准错误。
- 安全要求：涉及写入、外部访问或删除时应先确认范围。
- 兼容性：扩展参数和提供方参数可能随已加载资源变化。

## 15.19 `--export`

- 用途：导出当前或指定会话。
- 适用场景：归档、评审和迁移。
- 查询方式：运行 `loushang --help` 核对 `--export` 的登记基线定义。
- 自动化要求：固定结果格式，并同时检查退出状态和标准错误。
- 安全要求：涉及写入、外部访问或删除时应先确认范围。
- 兼容性：扩展参数和提供方参数可能随已加载资源变化。

## 15.20 `--export-format`

- 用途：选择 html 或 jsonl。
- 适用场景：按人读或机读用途导出。
- 查询方式：运行 `loushang --help` 核对 `--export-format` 的登记基线定义。
- 自动化要求：固定结果格式，并同时检查退出状态和标准错误。
- 安全要求：涉及写入、外部访问或删除时应先确认范围。
- 兼容性：扩展参数和提供方参数可能随已加载资源变化。

## 15.21 `--export-result-format`

- 用途：选择导出命令结果的 text 或 json。
- 适用场景：脚本获取导出状态。
- 查询方式：运行 `loushang --help` 核对 `--export-result-format` 的登记基线定义。
- 自动化要求：固定结果格式，并同时检查退出状态和标准错误。
- 安全要求：涉及写入、外部访问或删除时应先确认范围。
- 兼容性：扩展参数和提供方参数可能随已加载资源变化。

## 15.22 `--command`

- 用途：执行已发现命令。
- 适用场景：非交互调用 slash 类命令。
- 查询方式：运行 `loushang --help` 核对 `--command` 的登记基线定义。
- 自动化要求：固定结果格式，并同时检查退出状态和标准错误。
- 安全要求：涉及写入、外部访问或删除时应先确认范围。
- 兼容性：扩展参数和提供方参数可能随已加载资源变化。

## 15.23 `--command-args`

- 用途：为 command 提供参数文本。
- 适用场景：传递命令专用参数。
- 查询方式：运行 `loushang --help` 核对 `--command-args` 的登记基线定义。
- 自动化要求：固定结果格式，并同时检查退出状态和标准错误。
- 安全要求：涉及写入、外部访问或删除时应先确认范围。
- 兼容性：扩展参数和提供方参数可能随已加载资源变化。

## 15.24 `--command-result-format`

- 用途：选择 raw 或 json 命令结果。
- 适用场景：人读或集成处理。
- 查询方式：运行 `loushang --help` 核对 `--command-result-format` 的登记基线定义。
- 自动化要求：固定结果格式，并同时检查退出状态和标准错误。
- 安全要求：涉及写入、外部访问或删除时应先确认范围。
- 兼容性：扩展参数和提供方参数可能随已加载资源变化。

## 15.25 `--list-sessions-format`

- 用途：选择会话列表 TSV 或 JSON。
- 适用场景：终端浏览或自动化。
- 查询方式：运行 `loushang --help` 核对 `--list-sessions-format` 的登记基线定义。
- 自动化要求：固定结果格式，并同时检查退出状态和标准错误。
- 安全要求：涉及写入、外部访问或删除时应先确认范围。
- 兼容性：扩展参数和提供方参数可能随已加载资源变化。

## 15.26 `--source-info`

- 用途：显示资源与配置来源。
- 适用场景：排查层级覆盖问题。
- 查询方式：运行 `loushang --help` 核对 `--source-info` 的登记基线定义。
- 自动化要求：固定结果格式，并同时检查退出状态和标准错误。
- 安全要求：涉及写入、外部访问或删除时应先确认范围。
- 兼容性：扩展参数和提供方参数可能随已加载资源变化。

## 15.27 `--source-info-format`

- 用途：选择来源信息文本或 JSON。
- 适用场景：保存可审计快照。
- 查询方式：运行 `loushang --help` 核对 `--source-info-format` 的登记基线定义。
- 自动化要求：固定结果格式，并同时检查退出状态和标准错误。
- 安全要求：涉及写入、外部访问或删除时应先确认范围。
- 兼容性：扩展参数和提供方参数可能随已加载资源变化。

## 15.28 `--all-sessions`

- 用途：跨默认范围搜索会话。
- 适用场景：查找其他项目或目录的记录。
- 查询方式：运行 `loushang --help` 核对 `--all-sessions` 的登记基线定义。
- 自动化要求：固定结果格式，并同时检查退出状态和标准错误。
- 安全要求：涉及写入、外部访问或删除时应先确认范围。
- 兼容性：扩展参数和提供方参数可能随已加载资源变化。

## 15.29 `--session-index`

- 用途：启用或检查会话索引入口。
- 适用场景：加速大量记录发现。
- 查询方式：运行 `loushang --help` 核对 `--session-index` 的登记基线定义。
- 自动化要求：固定结果格式，并同时检查退出状态和标准错误。
- 安全要求：涉及写入、外部访问或删除时应先确认范围。
- 兼容性：扩展参数和提供方参数可能随已加载资源变化。

## 15.30 `--refresh-session-index`

- 用途：刷新会话索引。
- 适用场景：目录迁移或索引陈旧。
- 查询方式：运行 `loushang --help` 核对 `--refresh-session-index` 的登记基线定义。
- 自动化要求：固定结果格式，并同时检查退出状态和标准错误。
- 安全要求：涉及写入、外部访问或删除时应先确认范围。
- 兼容性：扩展参数和提供方参数可能随已加载资源变化。

## 15.31 `--session-cwd`

- 用途：按工作目录筛选会话。
- 适用场景：限定目标项目。
- 查询方式：运行 `loushang --help` 核对 `--session-cwd` 的登记基线定义。
- 自动化要求：固定结果格式，并同时检查退出状态和标准错误。
- 安全要求：涉及写入、外部访问或删除时应先确认范围。
- 兼容性：扩展参数和提供方参数可能随已加载资源变化。

## 15.32 `--session-name-filter`

- 用途：按名称筛选会话。
- 适用场景：定位业务任务。
- 查询方式：运行 `loushang --help` 核对 `--session-name-filter` 的登记基线定义。
- 自动化要求：固定结果格式，并同时检查退出状态和标准错误。
- 安全要求：涉及写入、外部访问或删除时应先确认范围。
- 兼容性：扩展参数和提供方参数可能随已加载资源变化。

## 15.33 `--session-parent`

- 用途：按父会话筛选。
- 适用场景：查看分支关系。
- 查询方式：运行 `loushang --help` 核对 `--session-parent` 的登记基线定义。
- 自动化要求：固定结果格式，并同时检查退出状态和标准错误。
- 安全要求：涉及写入、外部访问或删除时应先确认范围。
- 兼容性：扩展参数和提供方参数可能随已加载资源变化。

## 15.34 `--session-query`

- 用途：使用综合查询筛选会话。
- 适用场景：复杂历史检索。
- 查询方式：运行 `loushang --help` 核对 `--session-query` 的登记基线定义。
- 自动化要求：固定结果格式，并同时检查退出状态和标准错误。
- 安全要求：涉及写入、外部访问或删除时应先确认范围。
- 兼容性：扩展参数和提供方参数可能随已加载资源变化。

## 15.35 `--session-has-diagnostics`

- 用途：只列出含诊断会话。
- 适用场景：集中排查异常。
- 查询方式：运行 `loushang --help` 核对 `--session-has-diagnostics` 的登记基线定义。
- 自动化要求：固定结果格式，并同时检查退出状态和标准错误。
- 安全要求：涉及写入、外部访问或删除时应先确认范围。
- 兼容性：扩展参数和提供方参数可能随已加载资源变化。

## 15.36 `--session-no-diagnostics`

- 用途：只列出无诊断会话。
- 适用场景：筛选干净记录。
- 查询方式：运行 `loushang --help` 核对 `--session-no-diagnostics` 的登记基线定义。
- 自动化要求：固定结果格式，并同时检查退出状态和标准错误。
- 安全要求：涉及写入、外部访问或删除时应先确认范围。
- 兼容性：扩展参数和提供方参数可能随已加载资源变化。

## 15.37 `--session-limit`

- 用途：限制会话返回数量。
- 适用场景：控制列表规模。
- 查询方式：运行 `loushang --help` 核对 `--session-limit` 的登记基线定义。
- 自动化要求：固定结果格式，并同时检查退出状态和标准错误。
- 安全要求：涉及写入、外部访问或删除时应先确认范围。
- 兼容性：扩展参数和提供方参数可能随已加载资源变化。

## 15.38 `--fork`

- 用途：从指定会话创建分叉。
- 适用场景：尝试不同解决方案。
- 查询方式：运行 `loushang --help` 核对 `--fork` 的登记基线定义。
- 自动化要求：固定结果格式，并同时检查退出状态和标准错误。
- 安全要求：涉及写入、外部访问或删除时应先确认范围。
- 兼容性：扩展参数和提供方参数可能随已加载资源变化。

## 15.39 `--session-dir`

- 用途：指定会话存储目录。
- 适用场景：隔离项目或测试记录。
- 查询方式：运行 `loushang --help` 核对 `--session-dir` 的登记基线定义。
- 自动化要求：固定结果格式，并同时检查退出状态和标准错误。
- 安全要求：涉及写入、外部访问或删除时应先确认范围。
- 兼容性：扩展参数和提供方参数可能随已加载资源变化。

## 15.40 `--list-models`

- 用途：列出模型目录条目。
- 适用场景：选择和核对模型。
- 查询方式：运行 `loushang --help` 核对 `--list-models` 的登记基线定义。
- 自动化要求：固定结果格式，并同时检查退出状态和标准错误。
- 安全要求：涉及写入、外部访问或删除时应先确认范围。
- 兼容性：扩展参数和提供方参数可能随已加载资源变化。

## 15.41 `--list-models-format`

- 用途：选择模型列表 text 或 json。
- 适用场景：浏览或程序处理。
- 查询方式：运行 `loushang --help` 核对 `--list-models-format` 的登记基线定义。
- 自动化要求：固定结果格式，并同时检查退出状态和标准错误。
- 安全要求：涉及写入、外部访问或删除时应先确认范围。
- 兼容性：扩展参数和提供方参数可能随已加载资源变化。

## 15.42 `--models`

- 用途：指定额外模型目录。
- 适用场景：加载项目或私有模型。
- 查询方式：运行 `loushang --help` 核对 `--models` 的登记基线定义。
- 自动化要求：固定结果格式，并同时检查退出状态和标准错误。
- 安全要求：涉及写入、外部访问或删除时应先确认范围。
- 兼容性：扩展参数和提供方参数可能随已加载资源变化。

## 15.43 `--extension, -e`

- 用途：显式加载扩展。
- 适用场景：开发、测试或临时使用扩展。
- 查询方式：运行 `loushang --help` 核对 `--extension, -e` 的登记基线定义。
- 自动化要求：固定结果格式，并同时检查退出状态和标准错误。
- 安全要求：涉及写入、外部访问或删除时应先确认范围。
- 兼容性：扩展参数和提供方参数可能随已加载资源变化。

## 15.44 `--no-extensions, -ne`

- 用途：关闭扩展加载。
- 适用场景：隔离扩展故障。
- 查询方式：运行 `loushang --help` 核对 `--no-extensions, -ne` 的登记基线定义。
- 自动化要求：固定结果格式，并同时检查退出状态和标准错误。
- 安全要求：涉及写入、外部访问或删除时应先确认范围。
- 兼容性：扩展参数和提供方参数可能随已加载资源变化。

## 15.45 `--skill`

- 用途：显式选择技能。
- 适用场景：应用特定领域工作法。
- 查询方式：运行 `loushang --help` 核对 `--skill` 的登记基线定义。
- 自动化要求：固定结果格式，并同时检查退出状态和标准错误。
- 安全要求：涉及写入、外部访问或删除时应先确认范围。
- 兼容性：扩展参数和提供方参数可能随已加载资源变化。

## 15.46 `--no-skills, -ns`

- 用途：关闭技能发现或使用。
- 适用场景：隔离技能影响。
- 查询方式：运行 `loushang --help` 核对 `--no-skills, -ns` 的登记基线定义。
- 自动化要求：固定结果格式，并同时检查退出状态和标准错误。
- 安全要求：涉及写入、外部访问或删除时应先确认范围。
- 兼容性：扩展参数和提供方参数可能随已加载资源变化。

## 15.47 `--prompt-template`

- 用途：选择提示模板。
- 适用场景：复用标准任务提示。
- 查询方式：运行 `loushang --help` 核对 `--prompt-template` 的登记基线定义。
- 自动化要求：固定结果格式，并同时检查退出状态和标准错误。
- 安全要求：涉及写入、外部访问或删除时应先确认范围。
- 兼容性：扩展参数和提供方参数可能随已加载资源变化。

## 15.48 `--no-prompt-templates, -np`

- 用途：关闭提示模板。
- 适用场景：只使用原始用户提示。
- 查询方式：运行 `loushang --help` 核对 `--no-prompt-templates, -np` 的登记基线定义。
- 自动化要求：固定结果格式，并同时检查退出状态和标准错误。
- 安全要求：涉及写入、外部访问或删除时应先确认范围。
- 兼容性：扩展参数和提供方参数可能随已加载资源变化。

## 15.49 `--theme`

- 用途：选择终端主题。
- 适用场景：适配显示与可访问性。
- 查询方式：运行 `loushang --help` 核对 `--theme` 的登记基线定义。
- 自动化要求：固定结果格式，并同时检查退出状态和标准错误。
- 安全要求：涉及写入、外部访问或删除时应先确认范围。
- 兼容性：扩展参数和提供方参数可能随已加载资源变化。

## 15.50 `--no-themes`

- 用途：关闭主题资源。
- 适用场景：隔离主题加载问题。
- 查询方式：运行 `loushang --help` 核对 `--no-themes` 的登记基线定义。
- 自动化要求：固定结果格式，并同时检查退出状态和标准错误。
- 安全要求：涉及写入、外部访问或删除时应先确认范围。
- 兼容性：扩展参数和提供方参数可能随已加载资源变化。

## 15.51 `--system-prompt`

- 用途：替换系统提示。
- 适用场景：受控宿主定制行为。
- 查询方式：运行 `loushang --help` 核对 `--system-prompt` 的登记基线定义。
- 自动化要求：固定结果格式，并同时检查退出状态和标准错误。
- 安全要求：涉及写入、外部访问或删除时应先确认范围。
- 兼容性：扩展参数和提供方参数可能随已加载资源变化。

## 15.52 `--prompt, -p`

- 用途：提供主要任务提示。
- 适用场景：一次性或脚本任务。
- 查询方式：运行 `loushang --help` 核对 `--prompt, -p` 的登记基线定义。
- 自动化要求：固定结果格式，并同时检查退出状态和标准错误。
- 安全要求：涉及写入、外部访问或删除时应先确认范围。
- 兼容性：扩展参数和提供方参数可能随已加载资源变化。

## 15.53 `--append-system-prompt`

- 用途：追加系统提示内容。
- 适用场景：补充组织约束。
- 查询方式：运行 `loushang --help` 核对 `--append-system-prompt` 的登记基线定义。
- 自动化要求：固定结果格式，并同时检查退出状态和标准错误。
- 安全要求：涉及写入、外部访问或删除时应先确认范围。
- 兼容性：扩展参数和提供方参数可能随已加载资源变化。

## 15.54 `--verbose`

- 用途：启用更详细的人类输出。
- 适用场景：一般问题定位。
- 查询方式：运行 `loushang --help` 核对 `--verbose` 的登记基线定义。
- 自动化要求：固定结果格式，并同时检查退出状态和标准错误。
- 安全要求：涉及写入、外部访问或删除时应先确认范围。
- 兼容性：扩展参数和提供方参数可能随已加载资源变化。

## 15.55 `--debug`

- 用途：启用指定调试主题。
- 适用场景：组件级诊断。
- 查询方式：运行 `loushang --help` 核对 `--debug` 的登记基线定义。
- 自动化要求：固定结果格式，并同时检查退出状态和标准错误。
- 安全要求：涉及写入、外部访问或删除时应先确认范围。
- 兼容性：扩展参数和提供方参数可能随已加载资源变化。

## 15.56 `--debug-file`

- 用途：把调试信息写入文件。
- 适用场景：保存复现证据。
- 查询方式：运行 `loushang --help` 核对 `--debug-file` 的登记基线定义。
- 自动化要求：固定结果格式，并同时检查退出状态和标准错误。
- 安全要求：涉及写入、外部访问或删除时应先确认范围。
- 兼容性：扩展参数和提供方参数可能随已加载资源变化。

## 15.57 `--trace`

- 用途：启用指定跟踪主题。
- 适用场景：分析事件顺序。
- 查询方式：运行 `loushang --help` 核对 `--trace` 的登记基线定义。
- 自动化要求：固定结果格式，并同时检查退出状态和标准错误。
- 安全要求：涉及写入、外部访问或删除时应先确认范围。
- 兼容性：扩展参数和提供方参数可能随已加载资源变化。

## 15.58 `--trace-file`

- 用途：把跟踪信息写入文件。
- 适用场景：离线时序分析。
- 查询方式：运行 `loushang --help` 核对 `--trace-file` 的登记基线定义。
- 自动化要求：固定结果格式，并同时检查退出状态和标准错误。
- 安全要求：涉及写入、外部访问或删除时应先确认范围。
- 兼容性：扩展参数和提供方参数可能随已加载资源变化。

## 15.59 `--offline`

- 用途：禁止真实模型请求。
- 适用场景：静态、恢复和路由测试。
- 查询方式：运行 `loushang --help` 核对 `--offline` 的登记基线定义。
- 自动化要求：固定结果格式，并同时检查退出状态和标准错误。
- 安全要求：涉及写入、外部访问或删除时应先确认范围。
- 兼容性：扩展参数和提供方参数可能随已加载资源变化。

## 15.60 `--render-tool-events`

- 用途：在输出中呈现工具事件。
- 适用场景：观察和审计工具调用。
- 查询方式：运行 `loushang --help` 核对 `--render-tool-events` 的登记基线定义。
- 自动化要求：固定结果格式，并同时检查退出状态和标准错误。
- 安全要求：涉及写入、外部访问或删除时应先确认范围。
- 兼容性：扩展参数和提供方参数可能随已加载资源变化。

## 15.61 `--message`

- 用途：附加消息提示。
- 适用场景：构造多消息输入。
- 查询方式：运行 `loushang --help` 核对 `--message` 的登记基线定义。
- 自动化要求：固定结果格式，并同时检查退出状态和标准错误。
- 安全要求：涉及写入、外部访问或删除时应先确认范围。
- 兼容性：扩展参数和提供方参数可能随已加载资源变化。

## 15.62 `--tool`

- 用途：提供单个工具相关配置。
- 适用场景：细粒度工具装配。
- 查询方式：运行 `loushang --help` 核对 `--tool` 的登记基线定义。
- 自动化要求：固定结果格式，并同时检查退出状态和标准错误。
- 安全要求：涉及写入、外部访问或删除时应先确认范围。
- 兼容性：扩展参数和提供方参数可能随已加载资源变化。

## 15.63 `--no-context-files, -nc`

- 用途：关闭上下文文件发现。
- 适用场景：隔离项目指令影响。
- 查询方式：运行 `loushang --help` 核对 `--no-context-files, -nc` 的登记基线定义。
- 自动化要求：固定结果格式，并同时检查退出状态和标准错误。
- 安全要求：涉及写入、外部访问或删除时应先确认范围。
- 兼容性：扩展参数和提供方参数可能随已加载资源变化。

## 15.64 `--list-commands`

- 用途：列出内置及贡献命令。
- 适用场景：发现可用命令与来源。
- 查询方式：运行 `loushang --help` 核对 `--list-commands` 的登记基线定义。
- 自动化要求：固定结果格式，并同时检查退出状态和标准错误。
- 安全要求：涉及写入、外部访问或删除时应先确认范围。
- 兼容性：扩展参数和提供方参数可能随已加载资源变化。

## 15.65 `--list-commands-format`

- 用途：选择命令列表 TSV 或 JSON。
- 适用场景：终端浏览或集成。
- 查询方式：运行 `loushang --help` 核对 `--list-commands-format` 的登记基线定义。
- 自动化要求：固定结果格式，并同时检查退出状态和标准错误。
- 安全要求：涉及写入、外部访问或删除时应先确认范围。
- 兼容性：扩展参数和提供方参数可能随已加载资源变化。

## 15.66 `--list-diagnostics`

- 用途：列出诊断。
- 适用场景：检查运行与资源问题。
- 查询方式：运行 `loushang --help` 核对 `--list-diagnostics` 的登记基线定义。
- 自动化要求：固定结果格式，并同时检查退出状态和标准错误。
- 安全要求：涉及写入、外部访问或删除时应先确认范围。
- 兼容性：扩展参数和提供方参数可能随已加载资源变化。

## 15.67 `--list-diagnostics-format`

- 用途：选择诊断 TSV 或 JSON。
- 适用场景：保存或处理诊断。
- 查询方式：运行 `loushang --help` 核对 `--list-diagnostics-format` 的登记基线定义。
- 自动化要求：固定结果格式，并同时检查退出状态和标准错误。
- 安全要求：涉及写入、外部访问或删除时应先确认范围。
- 兼容性：扩展参数和提供方参数可能随已加载资源变化。

## 15.68 `--diagnostics-limit`

- 用途：限制诊断返回数量。
- 适用场景：控制大规模列表。
- 查询方式：运行 `loushang --help` 核对 `--diagnostics-limit` 的登记基线定义。
- 自动化要求：固定结果格式，并同时检查退出状态和标准错误。
- 安全要求：涉及写入、外部访问或删除时应先确认范围。
- 兼容性：扩展参数和提供方参数可能随已加载资源变化。

## 15.69 `--diag-export`

- 用途：导出诊断包。
- 适用场景：问题报告和离线分析。
- 查询方式：运行 `loushang --help` 核对 `--diag-export` 的登记基线定义。
- 自动化要求：固定结果格式，并同时检查退出状态和标准错误。
- 安全要求：涉及写入、外部访问或删除时应先确认范围。
- 兼容性：扩展参数和提供方参数可能随已加载资源变化。

## 15.70 `--diag-output`

- 用途：设置诊断导出路径。
- 适用场景：明确归档位置。
- 查询方式：运行 `loushang --help` 核对 `--diag-output` 的登记基线定义。
- 自动化要求：固定结果格式，并同时检查退出状态和标准错误。
- 安全要求：涉及写入、外部访问或删除时应先确认范围。
- 兼容性：扩展参数和提供方参数可能随已加载资源变化。

## 15.71 `--list-skills`

- 用途：列出技能。
- 适用场景：发现可复用工作资产。
- 查询方式：运行 `loushang --help` 核对 `--list-skills` 的登记基线定义。
- 自动化要求：固定结果格式，并同时检查退出状态和标准错误。
- 安全要求：涉及写入、外部访问或删除时应先确认范围。
- 兼容性：扩展参数和提供方参数可能随已加载资源变化。

## 15.72 `--list-skills-format`

- 用途：选择技能列表 TSV 或 JSON。
- 适用场景：浏览或自动化。
- 查询方式：运行 `loushang --help` 核对 `--list-skills-format` 的登记基线定义。
- 自动化要求：固定结果格式，并同时检查退出状态和标准错误。
- 安全要求：涉及写入、外部访问或删除时应先确认范围。
- 兼容性：扩展参数和提供方参数可能随已加载资源变化。

## 15.73 `--enable-skill`

- 用途：启用项目技能。
- 适用场景：持久化项目选择。
- 查询方式：运行 `loushang --help` 核对 `--enable-skill` 的登记基线定义。
- 自动化要求：固定结果格式，并同时检查退出状态和标准错误。
- 安全要求：涉及写入、外部访问或删除时应先确认范围。
- 兼容性：扩展参数和提供方参数可能随已加载资源变化。

## 15.74 `--disable-skill`

- 用途：禁用项目技能。
- 适用场景：控制技能来源。
- 查询方式：运行 `loushang --help` 核对 `--disable-skill` 的登记基线定义。
- 自动化要求：固定结果格式，并同时检查退出状态和标准错误。
- 安全要求：涉及写入、外部访问或删除时应先确认范围。
- 兼容性：扩展参数和提供方参数可能随已加载资源变化。

## 15.75 `--list-plugins`

- 用途：列出插件。
- 适用场景：检查来源与启用状态。
- 查询方式：运行 `loushang --help` 核对 `--list-plugins` 的登记基线定义。
- 自动化要求：固定结果格式，并同时检查退出状态和标准错误。
- 安全要求：涉及写入、外部访问或删除时应先确认范围。
- 兼容性：扩展参数和提供方参数可能随已加载资源变化。

## 15.76 `--list-plugins-format`

- 用途：选择插件列表 TSV 或 JSON。
- 适用场景：人工或审计处理。
- 查询方式：运行 `loushang --help` 核对 `--list-plugins-format` 的登记基线定义。
- 自动化要求：固定结果格式，并同时检查退出状态和标准错误。
- 安全要求：涉及写入、外部访问或删除时应先确认范围。
- 兼容性：扩展参数和提供方参数可能随已加载资源变化。

## 15.77 `--list-packages`

- 用途：列出软件包。
- 适用场景：检查可复用资产。
- 查询方式：运行 `loushang --help` 核对 `--list-packages` 的登记基线定义。
- 自动化要求：固定结果格式，并同时检查退出状态和标准错误。
- 安全要求：涉及写入、外部访问或删除时应先确认范围。
- 兼容性：扩展参数和提供方参数可能随已加载资源变化。

## 15.78 `--list-packages-format`

- 用途：选择 text、tsv 或 json。
- 适用场景：匹配消费方格式。
- 查询方式：运行 `loushang --help` 核对 `--list-packages-format` 的登记基线定义。
- 自动化要求：固定结果格式，并同时检查退出状态和标准错误。
- 安全要求：涉及写入、外部访问或删除时应先确认范围。
- 兼容性：扩展参数和提供方参数可能随已加载资源变化。

## 15.79 `--package-catalog`

- 用途：指定包目录。
- 适用场景：使用项目或组织包源。
- 查询方式：运行 `loushang --help` 核对 `--package-catalog` 的登记基线定义。
- 自动化要求：固定结果格式，并同时检查退出状态和标准错误。
- 安全要求：涉及写入、外部访问或删除时应先确认范围。
- 兼容性：扩展参数和提供方参数可能随已加载资源变化。

## 15.80 `--install-package`

- 用途：安装指定来源包。
- 适用场景：引入可复用资产。
- 查询方式：运行 `loushang --help` 核对 `--install-package` 的登记基线定义。
- 自动化要求：固定结果格式，并同时检查退出状态和标准错误。
- 安全要求：涉及写入、外部访问或删除时应先确认范围。
- 兼容性：扩展参数和提供方参数可能随已加载资源变化。

## 15.81 `--uninstall-package`

- 用途：卸载指定来源包。
- 适用场景：移除不再使用资产。
- 查询方式：运行 `loushang --help` 核对 `--uninstall-package` 的登记基线定义。
- 自动化要求：固定结果格式，并同时检查退出状态和标准错误。
- 安全要求：涉及写入、外部访问或删除时应先确认范围。
- 兼容性：扩展参数和提供方参数可能随已加载资源变化。

## 15.82 `--package-scope`

- 用途：选择 global 或 project 范围。
- 适用场景：控制安装影响面。
- 查询方式：运行 `loushang --help` 核对 `--package-scope` 的登记基线定义。
- 自动化要求：固定结果格式，并同时检查退出状态和标准错误。
- 安全要求：涉及写入、外部访问或删除时应先确认范围。
- 兼容性：扩展参数和提供方参数可能随已加载资源变化。

## 15.83 `--update-packages`

- 用途：更新已安装包集合。
- 适用场景：维护资产版本。
- 查询方式：运行 `loushang --help` 核对 `--update-packages` 的登记基线定义。
- 自动化要求：固定结果格式，并同时检查退出状态和标准错误。
- 安全要求：涉及写入、外部访问或删除时应先确认范围。
- 兼容性：扩展参数和提供方参数可能随已加载资源变化。

## 15.84 `--check-package-updates`

- 用途：检查可用包更新。
- 适用场景：升级前评估。
- 查询方式：运行 `loushang --help` 核对 `--check-package-updates` 的登记基线定义。
- 自动化要求：固定结果格式，并同时检查退出状态和标准错误。
- 安全要求：涉及写入、外部访问或删除时应先确认范围。
- 兼容性：扩展参数和提供方参数可能随已加载资源变化。

## 15.85 `--materialize-package`

- 用途：物化指定包内容。
- 适用场景：检查或定制资产。
- 查询方式：运行 `loushang --help` 核对 `--materialize-package` 的登记基线定义。
- 自动化要求：固定结果格式，并同时检查退出状态和标准错误。
- 安全要求：涉及写入、外部访问或删除时应先确认范围。
- 兼容性：扩展参数和提供方参数可能随已加载资源变化。

## 15.86 `--update-package`

- 用途：更新一个指定包。
- 适用场景：受控单包升级。
- 查询方式：运行 `loushang --help` 核对 `--update-package` 的登记基线定义。
- 自动化要求：固定结果格式，并同时检查退出状态和标准错误。
- 安全要求：涉及写入、外部访问或删除时应先确认范围。
- 兼容性：扩展参数和提供方参数可能随已加载资源变化。

## 15.87 `--remove-package`

- 用途：移除指定包记录或内容。
- 适用场景：清理资产。
- 查询方式：运行 `loushang --help` 核对 `--remove-package` 的登记基线定义。
- 自动化要求：固定结果格式，并同时检查退出状态和标准错误。
- 安全要求：涉及写入、外部访问或删除时应先确认范围。
- 兼容性：扩展参数和提供方参数可能随已加载资源变化。

## 15.88 `--add-plugin-source, --add-plugin`

- 用途：添加插件来源。
- 适用场景：引入可信插件目录。
- 查询方式：运行 `loushang --help` 核对 `--add-plugin-source, --add-plugin` 的登记基线定义。
- 自动化要求：固定结果格式，并同时检查退出状态和标准错误。
- 安全要求：涉及写入、外部访问或删除时应先确认范围。
- 兼容性：扩展参数和提供方参数可能随已加载资源变化。

## 15.89 `--remove-plugin-source, --remove-plugin`

- 用途：移除插件来源。
- 适用场景：停止发现指定来源。
- 查询方式：运行 `loushang --help` 核对 `--remove-plugin-source, --remove-plugin` 的登记基线定义。
- 自动化要求：固定结果格式，并同时检查退出状态和标准错误。
- 安全要求：涉及写入、外部访问或删除时应先确认范围。
- 兼容性：扩展参数和提供方参数可能随已加载资源变化。

## 15.90 `--enable-plugin`

- 用途：启用插件。
- 适用场景：让贡献进入项目解析。
- 查询方式：运行 `loushang --help` 核对 `--enable-plugin` 的登记基线定义。
- 自动化要求：固定结果格式，并同时检查退出状态和标准错误。
- 安全要求：涉及写入、外部访问或删除时应先确认范围。
- 兼容性：扩展参数和提供方参数可能随已加载资源变化。

## 15.91 `--disable-plugin`

- 用途：禁用插件。
- 适用场景：隔离插件影响。
- 查询方式：运行 `loushang --help` 核对 `--disable-plugin` 的登记基线定义。
- 自动化要求：固定结果格式，并同时检查退出状态和标准错误。
- 安全要求：涉及写入、外部访问或删除时应先确认范围。
- 兼容性：扩展参数和提供方参数可能随已加载资源变化。

## 15.92 `--method`

- 用途：使用发现的方法指导一次任务。
- 适用场景：按工作契约执行。
- 查询方式：运行 `loushang --help` 核对 `--method` 的登记基线定义。
- 自动化要求：固定结果格式，并同时检查退出状态和标准错误。
- 安全要求：涉及写入、外部访问或删除时应先确认范围。
- 兼容性：扩展参数和提供方参数可能随已加载资源变化。

## 15.93 `--no-method`

- 用途：绕过配置的默认方法。
- 适用场景：显式普通任务。
- 查询方式：运行 `loushang --help` 核对 `--no-method` 的登记基线定义。
- 自动化要求：固定结果格式，并同时检查退出状态和标准错误。
- 安全要求：涉及写入、外部访问或删除时应先确认范围。
- 兼容性：扩展参数和提供方参数可能随已加载资源变化。

## 15.94 `--prompt-steps, -ps`

- 用途：运行提示步骤工作流文件。
- 适用场景：阶段化可重复任务。
- 查询方式：运行 `loushang --help` 核对 `--prompt-steps, -ps` 的登记基线定义。
- 自动化要求：固定结果格式，并同时检查退出状态和标准错误。
- 安全要求：涉及写入、外部访问或删除时应先确认范围。
- 兼容性：扩展参数和提供方参数可能随已加载资源变化。

## 15.95 `--work-log`

- 用途：写入 WorkOperation 与 WorkEvent。
- 适用场景：记录可追踪运行。
- 查询方式：运行 `loushang --help` 核对 `--work-log` 的登记基线定义。
- 自动化要求：固定结果格式，并同时检查退出状态和标准错误。
- 安全要求：涉及写入、外部访问或删除时应先确认范围。
- 兼容性：扩展参数和提供方参数可能随已加载资源变化。

## 15.96 `--work-log-inspect`

- 用途：检查工作日志文件。
- 适用场景：复盘运行与计划。
- 查询方式：运行 `loushang --help` 核对 `--work-log-inspect` 的登记基线定义。
- 自动化要求：固定结果格式，并同时检查退出状态和标准错误。
- 安全要求：涉及写入、外部访问或删除时应先确认范围。
- 兼容性：扩展参数和提供方参数可能随已加载资源变化。

## 15.97 `--work-log-run`

- 用途：按 run id 筛选工作日志。
- 适用场景：定位单次运行。
- 查询方式：运行 `loushang --help` 核对 `--work-log-run` 的登记基线定义。
- 自动化要求：固定结果格式，并同时检查退出状态和标准错误。
- 安全要求：涉及写入、外部访问或删除时应先确认范围。
- 兼容性：扩展参数和提供方参数可能随已加载资源变化。

## 15.98 `--work-log-inspect-format`

- 用途：选择 text、json、plans 或 plans-json。
- 适用场景：匹配阅读与集成用途。
- 查询方式：运行 `loushang --help` 核对 `--work-log-inspect-format` 的登记基线定义。
- 自动化要求：固定结果格式，并同时检查退出状态和标准错误。
- 安全要求：涉及写入、外部访问或删除时应先确认范围。
- 兼容性：扩展参数和提供方参数可能随已加载资源变化。

## 15.99 `--list-methods`

- 用途：列出方法。
- 适用场景：发现工作契约。
- 查询方式：运行 `loushang --help` 核对 `--list-methods` 的登记基线定义。
- 自动化要求：固定结果格式，并同时检查退出状态和标准错误。
- 安全要求：涉及写入、外部访问或删除时应先确认范围。
- 兼容性：扩展参数和提供方参数可能随已加载资源变化。

## 15.100 `--list-methods-format`

- 用途：选择方法列表 TSV 或 JSON。
- 适用场景：浏览或自动化。
- 查询方式：运行 `loushang --help` 核对 `--list-methods-format` 的登记基线定义。
- 自动化要求：固定结果格式，并同时检查退出状态和标准错误。
- 安全要求：涉及写入、外部访问或删除时应先确认范围。
- 兼容性：扩展参数和提供方参数可能随已加载资源变化。

## 15.101 `--show-method`

- 用途：显示指定方法。
- 适用场景：执行前审查方法。
- 查询方式：运行 `loushang --help` 核对 `--show-method` 的登记基线定义。
- 自动化要求：固定结果格式，并同时检查退出状态和标准错误。
- 安全要求：涉及写入、外部访问或删除时应先确认范围。
- 兼容性：扩展参数和提供方参数可能随已加载资源变化。

## 15.102 `--show-method-format`

- 用途：选择方法 text 或 json。
- 适用场景：阅读或集成。
- 查询方式：运行 `loushang --help` 核对 `--show-method-format` 的登记基线定义。
- 自动化要求：固定结果格式，并同时检查退出状态和标准错误。
- 安全要求：涉及写入、外部访问或删除时应先确认范围。
- 兼容性：扩展参数和提供方参数可能随已加载资源变化。

## 15.103 `--show-method-plan`

- 用途：显示方法计划。
- 适用场景：确认阶段和产物。
- 查询方式：运行 `loushang --help` 核对 `--show-method-plan` 的登记基线定义。
- 自动化要求：固定结果格式，并同时检查退出状态和标准错误。
- 安全要求：涉及写入、外部访问或删除时应先确认范围。
- 兼容性：扩展参数和提供方参数可能随已加载资源变化。

## 15.104 `--show-method-plan-format`

- 用途：选择计划 text 或 json。
- 适用场景：阅读或处理计划。
- 查询方式：运行 `loushang --help` 核对 `--show-method-plan-format` 的登记基线定义。
- 自动化要求：固定结果格式，并同时检查退出状态和标准错误。
- 安全要求：涉及写入、外部访问或删除时应先确认范围。
- 兼容性：扩展参数和提供方参数可能随已加载资源变化。

# 第15章附录 交互命令参考

下列命令是登记基线用户文档中列出的主要交互命令。

## 15.A.1 `/settings`
- 功能：查看或修改设置。
- 使用位置：交互式会话输入区。
- 来源核对：运行 `loushang --list-commands`。
- 注意：扩展或其他资产可能贡献同名或附加命令，应检查来源。

## 15.A.2 `/model`
- 功能：查看和选择当前模型。
- 使用位置：交互式会话输入区。
- 来源核对：运行 `loushang --list-commands`。
- 注意：扩展或其他资产可能贡献同名或附加命令，应检查来源。

## 15.A.3 `/scoped-models`
- 功能：查看不同范围解析出的模型。
- 使用位置：交互式会话输入区。
- 来源核对：运行 `loushang --list-commands`。
- 注意：扩展或其他资产可能贡献同名或附加命令，应检查来源。

## 15.A.4 `/export`
- 功能：导出当前会话。
- 使用位置：交互式会话输入区。
- 来源核对：运行 `loushang --list-commands`。
- 注意：扩展或其他资产可能贡献同名或附加命令，应检查来源。

## 15.A.5 `/import`
- 功能：从 JSONL 导入并恢复会话。
- 使用位置：交互式会话输入区。
- 来源核对：运行 `loushang --list-commands`。
- 注意：扩展或其他资产可能贡献同名或附加命令，应检查来源。

## 15.A.6 `/share`
- 功能：使用可用共享入口处理会话。
- 使用位置：交互式会话输入区。
- 来源核对：运行 `loushang --list-commands`。
- 注意：扩展或其他资产可能贡献同名或附加命令，应检查来源。

## 15.A.7 `/copy`
- 功能：复制助手消息。
- 使用位置：交互式会话输入区。
- 来源核对：运行 `loushang --list-commands`。
- 注意：扩展或其他资产可能贡献同名或附加命令，应检查来源。

## 15.A.8 `/rename`
- 功能：重命名当前会话。
- 使用位置：交互式会话输入区。
- 来源核对：运行 `loushang --list-commands`。
- 注意：扩展或其他资产可能贡献同名或附加命令，应检查来源。

## 15.A.9 `/session`
- 功能：显示会话信息与统计。
- 使用位置：交互式会话输入区。
- 来源核对：运行 `loushang --list-commands`。
- 注意：扩展或其他资产可能贡献同名或附加命令，应检查来源。

## 15.A.10 `/terminal`
- 功能：查看或控制终端相关入口。
- 使用位置：交互式会话输入区。
- 来源核对：运行 `loushang --list-commands`。
- 注意：扩展或其他资产可能贡献同名或附加命令，应检查来源。

## 15.A.11 `/tools`
- 功能：查看或调整活动工具。
- 使用位置：交互式会话输入区。
- 来源核对：运行 `loushang --list-commands`。
- 注意：扩展或其他资产可能贡献同名或附加命令，应检查来源。

## 15.A.12 `/changelog`
- 功能：显示变更记录。
- 使用位置：交互式会话输入区。
- 来源核对：运行 `loushang --list-commands`。
- 注意：扩展或其他资产可能贡献同名或附加命令，应检查来源。

## 15.A.13 `/hotkeys`
- 功能：显示快捷键说明。
- 使用位置：交互式会话输入区。
- 来源核对：运行 `loushang --list-commands`。
- 注意：扩展或其他资产可能贡献同名或附加命令，应检查来源。

## 15.A.14 `/fork`
- 功能：从历史用户消息创建分叉。
- 使用位置：交互式会话输入区。
- 来源核对：运行 `loushang --list-commands`。
- 注意：扩展或其他资产可能贡献同名或附加命令，应检查来源。

## 15.A.15 `/clone`
- 功能：复制当前会话位置。
- 使用位置：交互式会话输入区。
- 来源核对：运行 `loushang --list-commands`。
- 注意：扩展或其他资产可能贡献同名或附加命令，应检查来源。

## 15.A.16 `/tree`
- 功能：导航会话树和分支。
- 使用位置：交互式会话输入区。
- 来源核对：运行 `loushang --list-commands`。
- 注意：扩展或其他资产可能贡献同名或附加命令，应检查来源。

## 15.A.17 `/new`
- 功能：在当前上下文新建空会话，不接受参数。
- 使用位置：交互式会话输入区。
- 来源核对：运行 `loushang --list-commands`。
- 注意：扩展或其他资产可能贡献同名或附加命令，应检查来源。

## 15.A.18 `/compact`
- 功能：手动压缩会话上下文。
- 使用位置：交互式会话输入区。
- 来源核对：运行 `loushang --list-commands`。
- 注意：扩展或其他资产可能贡献同名或附加命令，应检查来源。

## 15.A.19 `/resume`
- 功能：恢复其他会话。
- 使用位置：交互式会话输入区。
- 来源核对：运行 `loushang --list-commands`。
- 注意：扩展或其他资产可能贡献同名或附加命令，应检查来源。

## 15.A.20 `/delete`
- 功能：选择并删除历史会话，不删除当前活跃会话。
- 使用位置：交互式会话输入区。
- 来源核对：运行 `loushang --list-commands`。
- 注意：扩展或其他资产可能贡献同名或附加命令，应检查来源。

## 15.A.21 `/reload`
- 功能：重新加载资源。
- 使用位置：交互式会话输入区。
- 来源核对：运行 `loushang --list-commands`。
- 注意：扩展或其他资产可能贡献同名或附加命令，应检查来源。

## 15.A.22 `/quit`
- 功能：正常退出交互会话。
- 使用位置：交互式会话输入区。
- 来源核对：运行 `loushang --list-commands`。
- 注意：扩展或其他资产可能贡献同名或附加命令，应检查来源。
