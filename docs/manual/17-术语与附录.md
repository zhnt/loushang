# 第16章 术语表与附录

> 文档版本：V1.0.0

本章统一手册中的中英文术语，并给出版本、文档维护和打印说明。

## 16.1 术语表

### 16.1.1 Agent
- 定义：执行模型交互、工具调用和任务推进的运行核心。
- 使用建议：首次出现时可写为中文名称（Agent），后续按上下文使用中文或命令原文。
- 核对要求：涉及程序标识、参数和文件名时保持英文原文，不做意译。

### 16.1.2 Artifact
- 定义：对交付有意义的代码、报告、导出、计划或其他工作产物。
- 使用建议：首次出现时可写为中文名称（Artifact），后续按上下文使用中文或命令原文。
- 核对要求：涉及程序标识、参数和文件名时保持英文原文，不做意译。

### 16.1.3 Catalog
- 定义：描述模型、端点、能力、认证和价格元数据的目录。
- 使用建议：首次出现时可写为中文名称（Catalog），后续按上下文使用中文或命令原文。
- 核对要求：涉及程序标识、参数和文件名时保持英文原文，不做意译。

### 16.1.4 Channel
- 定义：宿主与 Agent 运行时之间传递消息和事件的边界协议。
- 使用建议：首次出现时可写为中文名称（Channel），后续按上下文使用中文或命令原文。
- 核对要求：涉及程序标识、参数和文件名时保持英文原文，不做意译。

### 16.1.5 CLI
- 定义：通过命令行参数、标准输入和标准输出使用 Loushang 的入口。
- 使用建议：首次出现时可写为中文名称（CLI），后续按上下文使用中文或命令原文。
- 核对要求：涉及程序标识、参数和文件名时保持英文原文，不做意译。

### 16.1.6 Coding
- 定义：Loushang V1.0.0 面向软件开发工作的主要产品面。
- 使用建议：首次出现时可写为中文名称（Coding），后续按上下文使用中文或命令原文。
- 核对要求：涉及程序标识、参数和文件名时保持英文原文，不做意译。

### 16.1.7 Compact
- 定义：把长上下文总结为可继续工作的压缩状态。
- 使用建议：首次出现时可写为中文名称（Compact），后续按上下文使用中文或命令原文。
- 核对要求：涉及程序标识、参数和文件名时保持英文原文，不做意译。

### 16.1.8 Continue
- 定义：恢复当前项目最近一次持久会话的快捷操作。
- 使用建议：首次出现时可写为中文名称（Continue），后续按上下文使用中文或命令原文。
- 核对要求：涉及程序标识、参数和文件名时保持英文原文，不做意译。

### 16.1.9 Diagnostic
- 定义：描述配置、运行、扩展或会话问题的结构化诊断信息。
- 使用建议：首次出现时可写为中文名称（Diagnostic），后续按上下文使用中文或命令原文。
- 核对要求：涉及程序标识、参数和文件名时保持英文原文，不做意译。

### 16.1.10 Endpoint
- 定义：具体模型服务的网络端点、区域、协议或部署通道。
- 使用建议：首次出现时可写为中文名称（Endpoint），后续按上下文使用中文或命令原文。
- 核对要求：涉及程序标识、参数和文件名时保持英文原文，不做意译。

### 16.1.11 Extension
- 定义：为会话贡献钩子、工具、资源、命令或参数的项目级 Python 代码。
- 使用建议：首次出现时可写为中文名称（Extension），后续按上下文使用中文或命令原文。
- 核对要求：涉及程序标识、参数和文件名时保持英文原文，不做意译。

### 16.1.12 Fork
- 定义：从历史消息节点创建新的会话分支。
- 使用建议：首次出现时可写为中文名称（Fork），后续按上下文使用中文或命令原文。
- 核对要求：涉及程序标识、参数和文件名时保持英文原文，不做意译。

### 16.1.13 JSONL
- 定义：每行一个 JSON 对象的流式记录格式。
- 使用建议：首次出现时可写为中文名称（JSONL），后续按上下文使用中文或命令原文。
- 核对要求：涉及程序标识、参数和文件名时保持英文原文，不做意译。

### 16.1.14 Manifest
- 定义：声明扩展身份、权限、依赖和预期贡献的元数据文件。
- 使用建议：首次出现时可写为中文名称（Manifest），后续按上下文使用中文或命令原文。
- 核对要求：涉及程序标识、参数和文件名时保持英文原文，不做意译。

### 16.1.15 Method
- 定义：定义角色、阶段、流程、约束、产物和验收的结构化工作契约。
- 使用建议：首次出现时可写为中文名称（Method），后续按上下文使用中文或命令原文。
- 核对要求：涉及程序标识、参数和文件名时保持英文原文，不做意译。

### 16.1.16 Model
- 定义：由模型目录解析并通过具体端点调用的 AI 模型身份。
- 使用建议：首次出现时可写为中文名称（Model），后续按上下文使用中文或命令原文。
- 核对要求：涉及程序标识、参数和文件名时保持英文原文，不做意译。

### 16.1.17 Package
- 定义：可安装、更新、物化或移除的可复用 Loushang 资产集合。
- 使用建议：首次出现时可写为中文名称（Package），后续按上下文使用中文或命令原文。
- 核对要求：涉及程序标识、参数和文件名时保持英文原文，不做意译。

### 16.1.18 Plugin
- 定义：向项目或用户环境提供技能、扩展等资源的可复用插件。
- 使用建议：首次出现时可写为中文名称（Plugin），后续按上下文使用中文或命令原文。
- 核对要求：涉及程序标识、参数和文件名时保持英文原文，不做意译。

### 16.1.19 Preferred Endpoint
- 定义：同一模型存在多个端点时被目录标记为优先的唯一候选。
- 使用建议：首次出现时可写为中文名称（Preferred Endpoint），后续按上下文使用中文或命令原文。
- 核对要求：涉及程序标识、参数和文件名时保持英文原文，不做意译。

### 16.1.20 Prompt
- 定义：用户交给模型或工作流的任务说明与上下文。
- 使用建议：首次出现时可写为中文名称（Prompt），后续按上下文使用中文或命令原文。
- 核对要求：涉及程序标识、参数和文件名时保持英文原文，不做意译。

### 16.1.21 Provider
- 定义：提供模型服务、认证和协议语义的模型提供方身份。
- 使用建议：首次出现时可写为中文名称（Provider），后续按上下文使用中文或命令原文。
- 核对要求：涉及程序标识、参数和文件名时保持英文原文，不做意译。

### 16.1.22 Reload
- 定义：重新发现键位、扩展、技能、提示模板和主题等资源。
- 使用建议：首次出现时可写为中文名称（Reload），后续按上下文使用中文或命令原文。
- 核对要求：涉及程序标识、参数和文件名时保持英文原文，不做意译。

### 16.1.23 Resume
- 定义：通过标识、路径或交互选择器恢复指定会话。
- 使用建议：首次出现时可写为中文名称（Resume），后续按上下文使用中文或命令原文。
- 核对要求：涉及程序标识、参数和文件名时保持英文原文，不做意译。

### 16.1.24 RPC
- 定义：供外部宿主以请求响应方式控制 Loushang 的集成模式。
- 使用建议：首次出现时可写为中文名称（RPC），后续按上下文使用中文或命令原文。
- 核对要求：涉及程序标识、参数和文件名时保持英文原文，不做意译。

### 16.1.25 Session
- 定义：保存消息、工具事件、用量、诊断和关系的持久执行记录。
- 使用建议：首次出现时可写为中文名称（Session），后续按上下文使用中文或命令原文。
- 核对要求：涉及程序标识、参数和文件名时保持英文原文，不做意译。

### 16.1.26 Skill
- 定义：封装领域知识、操作流程和工具协作方式的可复用技能。
- 使用建议：首次出现时可写为中文名称（Skill），后续按上下文使用中文或命令原文。
- 核对要求：涉及程序标识、参数和文件名时保持英文原文，不做意译。

### 16.1.27 Source Info
- 定义：说明资源和配置最终来自哪个层级的来源信息。
- 使用建议：首次出现时可写为中文名称（Source Info），后续按上下文使用中文或命令原文。
- 核对要求：涉及程序标识、参数和文件名时保持英文原文，不做意译。

### 16.1.28 Thinking
- 定义：向支持模型表达推理强度偏好的请求参数。
- 使用建议：首次出现时可写为中文名称（Thinking），后续按上下文使用中文或命令原文。
- 核对要求：涉及程序标识、参数和文件名时保持英文原文，不做意译。

### 16.1.29 Tool
- 定义：在策略控制下提供给 Agent 的可执行能力。
- 使用建议：首次出现时可写为中文名称（Tool），后续按上下文使用中文或命令原文。
- 核对要求：涉及程序标识、参数和文件名时保持英文原文，不做意译。

### 16.1.30 Tool Call
- 定义：模型请求应用调用某个工具及其结构化参数的事件。
- 使用建议：首次出现时可写为中文名称（Tool Call），后续按上下文使用中文或命令原文。
- 核对要求：涉及程序标识、参数和文件名时保持英文原文，不做意译。

### 16.1.31 Tool Result
- 定义：应用执行工具后返回给模型的成功或错误结果。
- 使用建议：首次出现时可写为中文名称（Tool Result），后续按上下文使用中文或命令原文。
- 核对要求：涉及程序标识、参数和文件名时保持英文原文，不做意译。

### 16.1.32 Trace
- 定义：用于分析事件顺序和边界行为的详细跟踪记录。
- 使用建议：首次出现时可写为中文名称（Trace），后续按上下文使用中文或命令原文。
- 核对要求：涉及程序标识、参数和文件名时保持英文原文，不做意译。

### 16.1.33 TUI
- 定义：运行在终端中的全屏或 plain 交互式用户界面。
- 使用建议：首次出现时可写为中文名称（TUI），后续按上下文使用中文或命令原文。
- 核对要求：涉及程序标识、参数和文件名时保持英文原文，不做意译。

### 16.1.34 Usage
- 定义：模型请求的输入、输出、缓存或其他计量信息。
- 使用建议：首次出现时可写为中文名称（Usage），后续按上下文使用中文或命令原文。
- 核对要求：涉及程序标识、参数和文件名时保持英文原文，不做意译。

### 16.1.35 Work Event
- 定义：表示工作计划、阶段或操作状态变化的结构化事件。
- 使用建议：首次出现时可写为中文名称（Work Event），后续按上下文使用中文或命令原文。
- 核对要求：涉及程序标识、参数和文件名时保持英文原文，不做意译。

### 16.1.36 Work Log
- 定义：按 JSONL 记录 WorkOperation 与 WorkEvent 的运行日志。
- 使用建议：首次出现时可写为中文名称（Work Log），后续按上下文使用中文或命令原文。
- 核对要求：涉及程序标识、参数和文件名时保持英文原文，不做意译。

### 16.1.37 Work Operation
- 定义：一次受方法或任务目标约束的工作运行实体。
- 使用建议：首次出现时可写为中文名称（Work Operation），后续按上下文使用中文或命令原文。
- 核对要求：涉及程序标识、参数和文件名时保持英文原文，不做意译。

### 16.1.38 Worktree
- 定义：Git 提供的独立工作目录，可用于隔离并行分支任务。
- 使用建议：首次出现时可写为中文名称（Worktree），后续按上下文使用中文或命令原文。
- 核对要求：涉及程序标识、参数和文件名时保持英文原文，不做意译。

## 16.2 文档版本说明

- 软件登记文档版本：V1.0.0。
- 文档发布日期：2026年7月。
- 软件名称、版本号、源代码页眉和登记申请表应保持一致。
- 本手册描述当前 coding 产品面和 loushang.ai SDK，不把路线图写成当前功能。

## 16.3 文档维护

- CLI 参数变更后，应重新生成第15章并执行帮助输出交叉检查。
- 模型目录和提供方信息变化后，应更新模型路由章节。
- 会话、工具、扩展和方法语义变化后，应补充迁移与兼容说明。
- 每次发布应重新生成 HTML，并保存 validation.json 验收结果。

## 16.4 打印说明

- 使用 A4 纵向、单面、黑白打印。
- 打印比例设为 100%，边距设为无，关闭浏览器附加页眉页脚。
- 正文页码已经固定在每页右上角，不使用浏览器自动页码。
- 正文每页包含不少于 30 个显式内容行。
- 封面和目录作为前置页，正文从第1页连续编号。

## 16.5 最终核对

- 核对软件全称、简称、版本号和权利人信息。
- 核对所有截图、命令和示例均来自目标版本。
- 核对正文前30页与后30页连续、清晰且无空白页。
- 核对会话、日志、截图和诊断中不存在真实凭证。
- 核对 PDF 或打印件，而不是只核对 Markdown 源文件。

## 16.6 文档结束

本手册到此结束。使用过程中应以目标安装环境的帮助、诊断和实际验证结果为准。
如发现文档与程序行为不一致，应先保存复现证据，再修正文档或程序基线。
任何完成声明都应能够追溯到实际命令、文件差异、测试结果或人工确认。
