# 第14章 常见问题与故障排除

> 文档版本：V1.0.0

本章按照“现象、可能原因、处理步骤、验证和注意事项”的顺序给出排查方法。

排查时应先保存现场，再缩小问题范围；不要为获得成功结果而删除失败证据。

## 14.1 找不到 loushang 命令

### 现象与原因
虚拟环境未激活、editable 安装失败或 PATH 指向错误环境。

### 处理步骤
1. 激活项目 .venv。
2. 重新执行 editable 安装。
3. 检查 which loushang。

### 验证
- 重新执行与“找不到 loushang 命令”直接相关的最小命令。
- 检查退出状态、标准错误、诊断列表和实际文件状态。
- 修复后再执行原始任务，确认问题没有被临时绕过。

### 注意事项
- 不要直接修改系统 PATH 掩盖安装问题。
- 若仍不能解决，应导出脱敏后的诊断和最小复现步骤。
- 问题报告应包含环境、模型键、工作目录类型和发生时间。

## 14.2 Python 版本过低

### 现象与原因
当前解释器低于项目要求的 Python 3.11。

### 处理步骤
1. 运行 python --version。
2. 使用兼容解释器重建 .venv。
3. 重新安装依赖。

### 验证
- 重新执行与“Python 版本过低”直接相关的最小命令。
- 检查退出状态、标准错误、诊断列表和实际文件状态。
- 修复后再执行原始任务，确认问题没有被临时绕过。

### 注意事项
- 不要在旧虚拟环境上原地替换解释器。
- 若仍不能解决，应导出脱敏后的诊断和最小复现步骤。
- 问题报告应包含环境、模型键、工作目录类型和发生时间。

## 14.3 依赖下载失败

### 现象与原因
网络、代理、证书或包索引不可用。

### 处理步骤
1. 检查 DNS 和代理。
2. 确认企业证书。
3. 重试 uv pip install。

### 验证
- 重新执行与“依赖下载失败”直接相关的最小命令。
- 检查退出状态、标准错误、诊断列表和实际文件状态。
- 修复后再执行原始任务，确认问题没有被临时绕过。

### 注意事项
- 不要关闭证书校验作为长期方案。
- 若仍不能解决，应导出脱敏后的诊断和最小复现步骤。
- 问题报告应包含环境、模型键、工作目录类型和发生时间。

## 14.4 模型列表为空

### 现象与原因
模型目录未加载、目录结构无效或来源被覆盖。

### 处理步骤
1. 运行 --source-info。
2. 检查 models 目录 JSON。
3. 使用 --list-models-format json。

### 验证
- 重新执行与“模型列表为空”直接相关的最小命令。
- 检查退出状态、标准错误、诊断列表和实际文件状态。
- 修复后再执行原始任务，确认问题没有被临时绕过。

### 注意事项
- 不要用未知旧目录覆盖内置 catalog。
- 若仍不能解决，应导出脱敏后的诊断和最小复现步骤。
- 问题报告应包含环境、模型键、工作目录类型和发生时间。

## 14.5 模型键不存在

### 现象与原因
输入的 provider、endpoint 或 model 标识与目录不一致。

### 处理步骤
1. 复制 --list-models 输出。
2. 使用规范完整键。
3. 检查拼写和冒号位置。

### 验证
- 重新执行与“模型键不存在”直接相关的最小命令。
- 检查退出状态、标准错误、诊断列表和实际文件状态。
- 修复后再执行原始任务，确认问题没有被临时绕过。

### 注意事项
- 不要根据展示名猜测模型键。
- 若仍不能解决，应导出脱敏后的诊断和最小复现步骤。
- 问题报告应包含环境、模型键、工作目录类型和发生时间。

## 14.6 模型选择歧义

### 现象与原因
同一 provider/model 匹配多个 endpoint 且没有唯一 preferred。

### 处理步骤
1. 查看错误候选。
2. 选择 provider:endpoint:model。
3. 验证端点区域。

### 验证
- 重新执行与“模型选择歧义”直接相关的最小命令。
- 检查退出状态、标准错误、诊断列表和实际文件状态。
- 修复后再执行原始任务，确认问题没有被临时绕过。

### 注意事项
- 不要任意选择第一个候选。
- 若仍不能解决，应导出脱敏后的诊断和最小复现步骤。
- 问题报告应包含环境、模型键、工作目录类型和发生时间。

## 14.7 认证失败

### 现象与原因
环境变量缺失、Key 无效、权限不足或端点认证方式不匹配。

### 处理步骤
1. 检查目录声明的变量名。
2. 验证 Key 范围。
3. 运行最小请求。

### 验证
- 重新执行与“认证失败”直接相关的最小命令。
- 检查退出状态、标准错误、诊断列表和实际文件状态。
- 修复后再执行原始任务，确认问题没有被临时绕过。

### 注意事项
- 不要把 Key 写进诊断或问题描述。
- 若仍不能解决，应导出脱敏后的诊断和最小复现步骤。
- 问题报告应包含环境、模型键、工作目录类型和发生时间。

## 14.8 请求超时

### 现象与原因
网络不稳定、端点负载、模型延迟或超时设置不合适。

### 处理步骤
1. 缩小提示与工具范围。
2. 检查网络和端点状态。
3. 按幂等策略有限重试。

### 验证
- 重新执行与“请求超时”直接相关的最小命令。
- 检查退出状态、标准错误、诊断列表和实际文件状态。
- 修复后再执行原始任务，确认问题没有被临时绕过。

### 注意事项
- 写操作不能盲目重试。
- 若仍不能解决，应导出脱敏后的诊断和最小复现步骤。
- 问题报告应包含环境、模型键、工作目录类型和发生时间。

## 14.9 模型不支持工具

### 现象与原因
目录能力声明、模型真实能力或协议适配不匹配。

### 处理步骤
1. 运行工具 smoke 示例。
2. 选择支持工具的模型。
3. 检查适配诊断。

### 验证
- 重新执行与“模型不支持工具”直接相关的最小命令。
- 检查退出状态、标准错误、诊断列表和实际文件状态。
- 修复后再执行原始任务，确认问题没有被临时绕过。

### 注意事项
- 不要用提示词模拟工具执行证据。
- 若仍不能解决，应导出脱敏后的诊断和最小复现步骤。
- 问题报告应包含环境、模型键、工作目录类型和发生时间。

## 14.10 工具未出现

### 现象与原因
工具被 --no-tools、only、no-builtin-tools 或扩展策略关闭。

### 处理步骤
1. 运行 /tools。
2. 检查启动参数。
3. 执行 /tools reset。

### 验证
- 重新执行与“工具未出现”直接相关的最小命令。
- 检查退出状态、标准错误、诊断列表和实际文件状态。
- 修复后再执行原始任务，确认问题没有被临时绕过。

### 注意事项
- 恢复默认前先确认安全范围。
- 若仍不能解决，应导出脱敏后的诊断和最小复现步骤。
- 问题报告应包含环境、模型键、工作目录类型和发生时间。

## 14.11 工具调用被阻止

### 现象与原因
扩展守卫、权限策略或宿主审批拒绝了调用。

### 处理步骤
1. 查看阻止原因。
2. 确认调用目标。
3. 仅在授权后调整策略。

### 验证
- 重新执行与“工具调用被阻止”直接相关的最小命令。
- 检查退出状态、标准错误、诊断列表和实际文件状态。
- 修复后再执行原始任务，确认问题没有被临时绕过。

### 注意事项
- 不要通过其他工具绕过阻止。
- 若仍不能解决，应导出脱敏后的诊断和最小复现步骤。
- 问题报告应包含环境、模型键、工作目录类型和发生时间。

## 14.12 文件修改不符合预期

### 现象与原因
上下文过期、目标路径错误或修改范围过大。

### 处理步骤
1. 立即停止后续写入。
2. 查看 git diff。
3. 恢复前先保留用户变更。

### 验证
- 重新执行与“文件修改不符合预期”直接相关的最小命令。
- 检查退出状态、标准错误、诊断列表和实际文件状态。
- 修复后再执行原始任务，确认问题没有被临时绕过。

### 注意事项
- 不要使用 reset --hard 处理不明差异。
- 若仍不能解决，应导出脱敏后的诊断和最小复现步骤。
- 问题报告应包含环境、模型键、工作目录类型和发生时间。

## 14.13 测试命令失败

### 现象与原因
代码缺陷、环境缺依赖、工作目录错误或测试本身不稳定。

### 处理步骤
1. 记录完整命令。
2. 定位第一个根因失败。
3. 运行最小复现。

### 验证
- 重新执行与“测试命令失败”直接相关的最小命令。
- 检查退出状态、标准错误、诊断列表和实际文件状态。
- 修复后再执行原始任务，确认问题没有被临时绕过。

### 注意事项
- 不要删除失败测试来获得绿色结果。
- 若仍不能解决，应导出脱敏后的诊断和最小复现步骤。
- 问题报告应包含环境、模型键、工作目录类型和发生时间。

## 14.14 会话列表为空

### 现象与原因
session-dir 不一致、索引陈旧或当前项目范围不同。

### 处理步骤
1. 检查 --session-dir。
2. 使用 --all-sessions。
3. 刷新 session index。

### 验证
- 重新执行与“会话列表为空”直接相关的最小命令。
- 检查退出状态、标准错误、诊断列表和实际文件状态。
- 修复后再执行原始任务，确认问题没有被临时绕过。

### 注意事项
- 跨项目结果要核对工作目录。
- 若仍不能解决，应导出脱敏后的诊断和最小复现步骤。
- 问题报告应包含环境、模型键、工作目录类型和发生时间。

## 14.15 无法恢复会话

### 现象与原因
文件损坏、格式不兼容、路径不可读或标识错误。

### 处理步骤
1. 备份原文件。
2. 逐行检查 JSONL。
3. 尝试显式路径恢复。

### 验证
- 重新执行与“无法恢复会话”直接相关的最小命令。
- 检查退出状态、标准错误、诊断列表和实际文件状态。
- 修复后再执行原始任务，确认问题没有被临时绕过。

### 注意事项
- 不要直接覆盖损坏原件。
- 若仍不能解决，应导出脱敏后的诊断和最小复现步骤。
- 问题报告应包含环境、模型键、工作目录类型和发生时间。

## 14.16 恢复后上下文过期

### 现象与原因
代码库在会话外发生了提交、切换或文件修改。

### 处理步骤
1. 运行 git status。
2. 重新读取关键文件。
3. 重新运行验证。

### 验证
- 重新执行与“恢复后上下文过期”直接相关的最小命令。
- 检查退出状态、标准错误、诊断列表和实际文件状态。
- 修复后再执行原始任务，确认问题没有被临时绕过。

### 注意事项
- 会话恢复不会恢复 Git 工作树。
- 若仍不能解决，应导出脱敏后的诊断和最小复现步骤。
- 问题报告应包含环境、模型键、工作目录类型和发生时间。

## 14.17 TUI 显示错位

### 现象与原因
终端尺寸、宽字符计算、TERM 或远程终端能力不匹配。

### 处理步骤
1. 扩大窗口。
2. 检查 TERM。
3. 尝试 plain prompt loop。

### 验证
- 重新执行与“TUI 显示错位”直接相关的最小命令。
- 检查退出状态、标准错误、诊断列表和实际文件状态。
- 修复后再执行原始任务，确认问题没有被临时绕过。

### 注意事项
- 异常退出后可执行 reset 恢复终端。
- 若仍不能解决，应导出脱敏后的诊断和最小复现步骤。
- 问题报告应包含环境、模型键、工作目录类型和发生时间。

## 14.18 TUI 无法启动

### 现象与原因
stdio 不是 TTY、终端不兼容或依赖环境错误。

### 处理步骤
1. 检查终端交互性。
2. 使用 --no-tui 验证核心 CLI。
3. 查看诊断。

### 验证
- 重新执行与“TUI 无法启动”直接相关的最小命令。
- 检查退出状态、标准错误、诊断列表和实际文件状态。
- 修复后再执行原始任务，确认问题没有被临时绕过。

### 注意事项
- 管道场景会进入 plain 模式。
- 若仍不能解决，应导出脱敏后的诊断和最小复现步骤。
- 问题报告应包含环境、模型键、工作目录类型和发生时间。

## 14.19 扩展加载失败

### 现象与原因
Python 语法、依赖、Manifest 或 register 协议错误。

### 处理步骤
1. 运行扩展示例。
2. 查看 /extensions。
3. 禁用扩展后验证核心。

### 验证
- 重新执行与“扩展加载失败”直接相关的最小命令。
- 检查退出状态、标准错误、诊断列表和实际文件状态。
- 修复后再执行原始任务，确认问题没有被临时绕过。

### 注意事项
- 不要忽略扩展加载诊断。
- 若仍不能解决，应导出脱敏后的诊断和最小复现步骤。
- 问题报告应包含环境、模型键、工作目录类型和发生时间。

## 14.20 扩展修改后未生效

### 现象与原因
资源快照尚未 reload 或进程级依赖未更新。

### 处理步骤
1. 保存文件。
2. 执行 /reload。
3. 必要时重启进程。

### 验证
- 重新执行与“扩展修改后未生效”直接相关的最小命令。
- 检查退出状态、标准错误、诊断列表和实际文件状态。
- 修复后再执行原始任务，确认问题没有被临时绕过。

### 注意事项
- 不要在工具调用执行中强制 reload。
- 若仍不能解决，应导出脱敏后的诊断和最小复现步骤。
- 问题报告应包含环境、模型键、工作目录类型和发生时间。

## 14.21 技能未被发现

### 现象与原因
技能路径、启用状态、优先级或内容格式不正确。

### 处理步骤
1. 运行 --list-skills。
2. 检查 source-info。
3. enable 后 reload。

### 验证
- 重新执行与“技能未被发现”直接相关的最小命令。
- 检查退出状态、标准错误、诊断列表和实际文件状态。
- 修复后再执行原始任务，确认问题没有被临时绕过。

### 注意事项
- 来源冲突时不要静默覆盖。
- 若仍不能解决，应导出脱敏后的诊断和最小复现步骤。
- 问题报告应包含环境、模型键、工作目录类型和发生时间。

## 14.22 Method 被拒绝

### 现象与原因
当前模式为 TUI 或 RPC，或方法无法唯一解析。

### 处理步骤
1. 切换 prompt/print/json。
2. 运行 --list-methods。
3. 使用完整方法标识。

### 验证
- 重新执行与“Method 被拒绝”直接相关的最小命令。
- 检查退出状态、标准错误、诊断列表和实际文件状态。
- 修复后再执行原始任务，确认问题没有被临时绕过。

### 注意事项
- 不要假设被拒绝时仍会应用方法。
- 若仍不能解决，应导出脱敏后的诊断和最小复现步骤。
- 问题报告应包含环境、模型键、工作目录类型和发生时间。

## 14.23 Work Log 无法写入

### 现象与原因
路径无权限、父目录不存在或模式不支持。

### 处理步骤
1. 检查目标目录。
2. 切换一次性模式。
3. 使用项目内明确路径。

### 验证
- 重新执行与“Work Log 无法写入”直接相关的最小命令。
- 检查退出状态、标准错误、诊断列表和实际文件状态。
- 修复后再执行原始任务，确认问题没有被临时绕过。

### 注意事项
- TUI 和 RPC 不支持 --work-log。
- 若仍不能解决，应导出脱敏后的诊断和最小复现步骤。
- 问题报告应包含环境、模型键、工作目录类型和发生时间。

## 14.24 导出文件为空

### 现象与原因
会话目标错误、输出路径问题或导出在事件完成前触发。

### 处理步骤
1. 检查会话标识。
2. 等待最终事件。
3. 使用显式格式重试。

### 验证
- 重新执行与“导出文件为空”直接相关的最小命令。
- 检查退出状态、标准错误、诊断列表和实际文件状态。
- 修复后再执行原始任务，确认问题没有被临时绕过。

### 注意事项
- 保留失败文件用于诊断。
- 若仍不能解决，应导出脱敏后的诊断和最小复现步骤。
- 问题报告应包含环境、模型键、工作目录类型和发生时间。

## 14.25 JSON 输出无法解析

### 现象与原因
调试文本混入 stdout、进程被中断或调用方选择了文本格式。

### 处理步骤
1. 固定 json 格式。
2. 分离 stderr。
3. 检查退出状态。

### 验证
- 重新执行与“JSON 输出无法解析”直接相关的最小命令。
- 检查退出状态、标准错误、诊断列表和实际文件状态。
- 修复后再执行原始任务，确认问题没有被临时绕过。

### 注意事项
- 不要用正则表达式代替 JSON 解析。
- 若仍不能解决，应导出脱敏后的诊断和最小复现步骤。
- 问题报告应包含环境、模型键、工作目录类型和发生时间。

## 14.26 诊断文件过大

### 现象与原因
长期开启 trace、工具输出过多或缺少轮转。

### 处理步骤
1. 缩小调试主题。
2. 限制复现次数。
3. 按策略归档清理。

### 验证
- 重新执行与“诊断文件过大”直接相关的最小命令。
- 检查退出状态、标准错误、诊断列表和实际文件状态。
- 修复后再执行原始任务，确认问题没有被临时绕过。

### 注意事项
- 清理前先保留问题时间窗口。
- 若仍不能解决，应导出脱敏后的诊断和最小复现步骤。
- 问题报告应包含环境、模型键、工作目录类型和发生时间。

## 14.27 用量与账单不同

### 现象与原因
价格元数据过期、缓存计费、汇率或提供方账单口径不同。

### 处理步骤
1. 保存规范模型键。
2. 更新价格目录。
3. 与提供方账单核对。

### 验证
- 重新执行与“用量与账单不同”直接相关的最小命令。
- 检查退出状态、标准错误、诊断列表和实际文件状态。
- 修复后再执行原始任务，确认问题没有被临时绕过。

### 注意事项
- 估算值不能替代财务账单。
- 若仍不能解决，应导出脱敏后的诊断和最小复现步骤。
- 问题报告应包含环境、模型键、工作目录类型和发生时间。

## 14.28 命令名称冲突

### 现象与原因
扩展、技能、prompt 与内置命令使用了相同标识。

### 处理步骤
1. 运行 --list-commands。
2. 查看来源列。
3. 重命名非核心贡献。

### 验证
- 重新执行与“命令名称冲突”直接相关的最小命令。
- 检查退出状态、标准错误、诊断列表和实际文件状态。
- 修复后再执行原始任务，确认问题没有被临时绕过。

### 注意事项
- 不要覆盖安全相关内置命令。
- 若仍不能解决，应导出脱敏后的诊断和最小复现步骤。
- 问题报告应包含环境、模型键、工作目录类型和发生时间。

## 14.29 终端退出后状态异常

### 现象与原因
全屏程序未正常恢复终端模式。

### 处理步骤
1. 执行 reset。
2. 重新打开终端。
3. 保存复现诊断。

### 验证
- 重新执行与“终端退出后状态异常”直接相关的最小命令。
- 检查退出状态、标准错误、诊断列表和实际文件状态。
- 修复后再执行原始任务，确认问题没有被临时绕过。

### 注意事项
- 不要在异常终端继续输入秘密。
- 若仍不能解决，应导出脱敏后的诊断和最小复现步骤。
- 问题报告应包含环境、模型键、工作目录类型和发生时间。

## 14.30 版本号不一致

### 现象与原因
软件包、申请表、源代码页眉和用户手册使用了不同版本。

### 处理步骤
1. 确定唯一登记版本。
2. 同步所有材料。
3. 重新生成并逐项检查。

### 验证
- 重新执行与“版本号不一致”直接相关的最小命令。
- 检查退出状态、标准错误、诊断列表和实际文件状态。
- 修复后再执行原始任务，确认问题没有被临时绕过。

### 注意事项
- 有无字母 V 也应按申请表保持一致。
- 若仍不能解决，应导出脱敏后的诊断和最小复现步骤。
- 问题报告应包含环境、模型键、工作目录类型和发生时间。
